# 📘 **3.4 이수 완료 처리**

## ✔ url

**POST /edu/{id}/complete**

---

### ✔ 설명

특정 교육을 이수 처리한다.

교육 이수 조건:

- **영상 단위 이수 조건 충족** (모든 영상 pass_ratio 이상)
- **퀴즈가 있는 경우, 퀴즈 attempt 통과(passScore 이상)**

조건을 충족하지 못한 경우 이수 처리 불가하게 응답한다.

---

### ✔ 권한

`ROLE_USER`

---

# 📌 **Request**

| key  | 설명           | value 타입 | 옵션 | Nullable | 예시 |
| ---- | -------------- | ---------- | ---- | -------- | ---- |
| 없음 | 요청 바디 없음 | -          | -    | -        | -    |

---

# 📌 **Query Parameter**

| key  | 설명 | value 타입 | 옵션 | Nullable | 예시 |
| ---- | ---- | ---------- | ---- | -------- | ---- |
| 없음 | -    | -          | -    | -        | -    |

---

# 📌 **Response**

| key         | 설명                         | value 타입       | 옵션     | Nullable | 예시                    |
| ----------- | ---------------------------- | ---------------- | -------- | -------- | ----------------------- |
| status      | 교육 전체 이수 상태          | string           | required | false    | `"COMPLETED"`           |
| completedAt | 교육 전체 완료 시각          | string(datetime) | required | false    | `"2025-01-01T12:00:00"` |
| message     | (조건 미충족 시) 이유 메시지 | string           | optional | true     | `"퀴즈 미완료"`         |

---

# 📌 **Example — 성공**

```json
{
  "status": "COMPLETED",
  "completedAt": "2025-01-01T12:00:00"
}
```

---

# 📌 **Example — 실패 (조건 미충족)**

```json
{
  "status": "FAILED",
  "message": "영상 2개 중 1개가 아직 이수되지 않았습니다."
}
```

또는

```json
{
  "status": "FAILED",
  "message": "퀴즈가 아직 통과되지 않았습니다."
}
```

---

# 📌 **Status**

| status                        | response content         |
| ----------------------------- | ------------------------ |
| **200 OK**                    | 이수 처리 성공           |
| **400 Bad Request**           | 이수 조건 미충족         |
| **401 Unauthorized**          | 인증 실패                |
| **403 Forbidden**             | 해당 교육 이수 권한 없음 |
| **404 Not Found**             | 교육 존재하지 않음       |
| **500 Internal Server Error** | 서버 오류                |
