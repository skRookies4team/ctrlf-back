spring:
  datasource:
    url: jdbc:postgresql://keycloak-db:5432/db
    username: postgres
    password: postgres

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak-service:8090/realms/ctrlf

keycloak:
  admin:
    base-url: http://keycloak-service:8090
    realm: ctrlf
    client-id: infra-admin
    client-secret: changeme

app:
  rag:
    ai:
      base-url: http://192.168.0.112:8000
      token: changeme
      timeout-seconds: 10